# 🐝 Ферма Медовик — Инструкция по установке на Shared Hosting

## Требования
- Shared Hosting с поддержкой Python (например: Beget, TimeWeb, REG.RU)
- MariaDB или MySQL 5.7+
- Python 3.9+
- Доступ к файловому менеджеру или FTP/SFTP

---

## Шаг 1: Подготовка базы данных

### 1.1 Создайте базу данных в панели хостинга
1. Войдите в панель управления хостингом
2. Найдите раздел **"Базы данных MySQL/MariaDB"**
3. Создайте новую базу данных:
   - Имя БД: `fermamedovik` (или любое другое)
   - Создайте пользователя БД с полными правами
4. Запишите данные:
   ```
   Хост: localhost (или указанный хостингом)
   Имя БД: fermamedovik
   Пользователь: ваш_пользователь
   Пароль: ваш_пароль
   ```

---

## Шаг 2: Сборка фронтенда

### 2.1 На вашем компьютере (с Node.js)
```bash
# Перейдите в папку frontend
cd frontend

# Установите зависимости
yarn install

# Отредактируйте .env для продакшена
# REACT_APP_BACKEND_URL=https://fermamedovik.kz/api
# Или если API на поддомене:
# REACT_APP_BACKEND_URL=https://api.fermamedovik.kz

# Соберите проект
yarn build
```

### 2.2 Результат
После сборки появится папка `build/` со статическими файлами.

---

## Шаг 3: Загрузка файлов на хостинг

### 3.1 Структура папок на сервере
```
public_html/                    # Корневая папка сайта
├── index.html                  # Из build/
├── static/                     # Из build/static/
│   ├── css/
│   ├── js/
│   └── media/
├── favicon.ico
├── manifest.json
└── api/                        # Папка для бэкенда
    ├── server.py               # server_mariadb.py переименованный
    ├── requirements.txt        # requirements_mariadb.txt
    └── .htaccess               # Настройки Apache
```

### 3.2 Загрузите файлы
1. **Фронтенд**: Загрузите содержимое папки `build/` в `public_html/`
2. **Бэкенд**: Создайте папку `api/` и загрузите:
   - `server_mariadb.py` → переименуйте в `server.py`
   - `requirements_mariadb.txt` → переименуйте в `requirements.txt`

---

## Шаг 4: Настройка бэкенда

### 4.1 Отредактируйте server.py
Найдите блок конфигурации и измените:

```python
DB_CONFIG = {
    'host': 'localhost',              # Обычно localhost
    'user': 'ваш_пользователь',       # Пользователь БД
    'password': 'ваш_пароль',         # Пароль БД
    'database': 'fermamedovik',       # Имя БД
    'charset': 'utf8mb4',
    'cursorclass': pymysql.cursors.DictCursor
}
```

### 4.2 Установите зависимости Python
Через SSH или панель хостинга:
```bash
cd ~/public_html/api
pip install -r requirements.txt --user
# или
pip3 install -r requirements.txt --user
```

---

## Шаг 5: Настройка веб-сервера

### 5.1 Для Apache (.htaccess в public_html/)
Создайте файл `.htaccess` в `public_html/`:

```apache
# Включаем mod_rewrite
RewriteEngine On
RewriteBase /

# API запросы проксируем на Python
RewriteCond %{REQUEST_URI} ^/api/
RewriteRule ^api/(.*)$ http://127.0.0.1:8000/api/$1 [P,L]

# React Router - все остальные запросы на index.html
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
```

### 5.2 Если хостинг использует Passenger (REG.RU, Beget)
Создайте файл `passenger_wsgi.py` в `public_html/`:

```python
import sys
import os

# Путь к папке с бэкендом
sys.path.insert(0, os.path.dirname(__file__) + '/api')

# Импортируем FastAPI приложение
from server import app

# Для Passenger
application = app
```

---

## Шаг 6: Запуск бэкенда

### Вариант A: Через Supervisor (рекомендуется)
Если хостинг поддерживает Supervisor, создайте конфиг:

```ini
[program:fermamedovik]
directory=/home/username/public_html/api
command=/usr/bin/python3 -m uvicorn server:app --host 127.0.0.1 --port 8000
autostart=true
autorestart=true
user=username
```

### Вариант B: Через cron (как демон)
```bash
# Добавьте в crontab
@reboot cd /home/username/public_html/api && nohup python3 -m uvicorn server:app --host 127.0.0.1 --port 8000 &
```

### Вариант C: Passenger (автоматически)
Если используется Passenger, приложение запустится автоматически.

---

## Шаг 7: Инициализация данных

После запуска откройте в браузере:
```
https://fermamedovik.kz/api/seed
```
Это создаст начальные данные (категории и товары).

---

## Шаг 8: Проверка работы

1. **Главная страница**: https://fermamedovik.kz
2. **Админ-панель**: https://fermamedovik.kz/admin
   - Пароль: `secretboost1`
3. **API статус**: https://fermamedovik.kz/api/

---

## Возможные проблемы

### Ошибка 500 на API
- Проверьте логи Apache/Nginx
- Убедитесь, что Python и зависимости установлены
- Проверьте права доступа к файлам (644 для файлов, 755 для папок)

### Не работает подключение к БД
- Проверьте данные подключения в `server.py`
- Убедитесь, что пользователь БД имеет права на базу

### React Router не работает (404 на страницах)
- Проверьте `.htaccess`
- Убедитесь, что `mod_rewrite` включен

### CORS ошибки
В `server.py` замените `allow_origins=["*"]` на ваш домен:
```python
allow_origins=["https://fermamedovik.kz"]
```

---

## Контакты для поддержки
- WhatsApp: +7 708 321 45 71
- Telegram: @fermamedovik

---

## Файлы для деплоя
- `/app/deploy/server_mariadb.py` — бэкенд для MariaDB
- `/app/deploy/requirements_mariadb.txt` — зависимости Python
- `/app/frontend/build/` — собранный фронтенд (после `yarn build`)
